var phoneNumberRegex = require("./phone-number-regex");
var isValidMxPhoneNumber = require("./is-valid-mx-phone-number");

module.exports = {
  phoneNumberRegex: phoneNumberRegex,
  isValidMxPhoneNumber: isValidMxPhoneNumber,
};
